# Server
PORT=8080
# Options: development, preview, production
ENVIRONMENT=development

# Database
# For Neon: postgres://[user]:[password]@[host]/[dbname]?sslmode=require
# Local dev: postgresql://user:password@localhost:5432/carbuyer
# Preview: Use Neon preview branch connection string
# Production: Use Neon production connection string
DATABASE_URL=postgresql://user:password@localhost:5432/carbuyer

# Authentication
# Generate secure secret: openssl rand -base64 32
# MUST be different for each environment
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRATION_HOURS=24

# Anthropic API
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# CORS
# Supports comma-separated list for multiple origins
# Local: http://localhost:3000
# Preview: https://car-buyer-git-*.vercel.app,https://car-buyer-*.vercel.app
# Production: https://your-domain.com,https://www.your-domain.com
ALLOWED_ORIGINS=http://localhost:3000

# Rate Limiting
RATE_LIMIT_AUTH=5
RATE_LIMIT_API=100

# Mailgun (optional for now)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_WEBHOOK_SIGNING_KEY=
